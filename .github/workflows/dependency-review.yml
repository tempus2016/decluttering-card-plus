name: Dependency review

# Fails a pull request that introduces a dependency with a known vulnerability, or one
# under a licence this project will not ship. Only build tooling and lit are in scope
# today, and this is the gate that keeps it that way. Needs the repository's dependency
# graph enabled, or the action refuses to run at all.

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  dependency-review:
    name: Dependency review
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Review dependency changes
        uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          fail-on-severity: high
          # The card ships as one MIT-licensed bundle, so a strong-copyleft dependency pulled
          # into it cannot be distributed under those terms. None is in the tree today; this
          # keeps it that way. Listed by SPDX id.
          deny-licenses: AGPL-3.0, GPL-2.0, GPL-3.0
          comment-summary-in-pr: on-failure
