## What this changes

<!-- One or two sentences. What behaviour is different afterwards? -->

## Why

<!-- The problem being solved. Link the issue if there is one: Fixes #123 -->

## How it was verified

<!--
Delete what does not apply. "Builds" is not verification on its own — this card renders
inside Home Assistant, so say what you actually looked at.
-->

- [ ] `npm run lint`, `npm test` and `npm run build` pass
- [ ] `dist/decluttering-card-plus.js` is rebuilt and committed (CI fails otherwise)
- [ ] Tried in a real Home Assistant dashboard, in both normal and edit mode
- [ ] Checked the browser console for errors

## Anything to watch

<!-- Behaviour changes for existing configurations, new minimum HA version, docs that need updating. -->
